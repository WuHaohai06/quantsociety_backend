factor:
  name: cleaned_day_aggs_rank_ts_mean_close_3_v1
  expr: rank(ts_mean(col("close"), 3))
  freq: 1d
  universe: equities
  description: cleaned parquet 单数据源的 3 日均价截面排名示例

data_source:
  type: cleaned_parquet
  root: /home/yluel/share/projects/massive_parquet/cleaned_massive_data/us_stocks_sip/day_aggs_v1
  max_files: 5

backend:
  type: pandas

engine:
  enable_cache: true

materialization:
  lake_root: /tmp/factor_engine_demo_lake
  factor_id: cleaned_day_aggs_rank_ts_mean_close_3_v1
  description: 配置驱动计算到落盘示例
  expression: rank(ts_mean(col("close"), 3))