# Mock Generator

这个目录保存 `barra_model` 的 mock / demo 版本脚本，仅用于本地演示和测试，不属于主生产流程。

## 说明

- 这里的脚本默认沿用本地工作目录和示例路径
- 如果你在其他环境运行，请显式修改脚本里的默认路径或通过参数传入
- 主 README 只描述生产链路，mock 相关说明统一放在这里

## 文件

- `alpha_generate.py`
- `factor_generate.py`
- `optimization.py`
